"""
Show how to implement the stack ADT using only a priority queue and
one additional integer instance variable.

-> Lets x be a counter that starts from 0
-> When you add element, assign it under a key x.
-> After that decrement x (x -= 1).

"""
