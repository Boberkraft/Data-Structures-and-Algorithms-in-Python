"""
Give an alternate description of the in-place heap-sort algorithm using
the standard minimum-oriented priority queue (instead of a maximumoriented
one).

when insering an item to the sorted part, you store it on the left part of the 
array, and every item in heap one intex to right
"""
