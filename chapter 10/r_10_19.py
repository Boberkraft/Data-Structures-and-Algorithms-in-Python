"""
 Describe how a sorted list implemented as a doubly linked list could be
used to implement the sorted map ADT.


def get(search_key):
    for key, item in self._table:
        if search == key:
            return value
    raise ValueError
    
def set(key, value):
    for key, item in self._table:
        if search == key:
            raise ValueError
    self.add_last(key, value)
"""