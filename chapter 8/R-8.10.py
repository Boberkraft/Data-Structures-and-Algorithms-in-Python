"""
Give a direct implementation of the num children method within the class
BinaryTree.

Just copied num_children() from LinkedBinaryTree to BinaryTree 
withoud validation.
In LinkedBinaryTree 
    def num_children(self, p):
        node = self._validate(p)
        return super().num_children(node)
"""