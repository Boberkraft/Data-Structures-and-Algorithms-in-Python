"""
Show a tree achieving the worst-case running time for algorithm depth.

Basically a binary tree with only only nodes on left branches.
if depth() is invoded on the leaf then its O(n)

root -> node -> node -> node -> node -> leaf
"""

