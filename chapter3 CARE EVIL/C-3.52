Show that the summation ∑n
i=1 logi is Ω(nlogn).

consider

log(1) + log(2) + log(3) + log(4) + log(n) >= c * log(n)

log(1) + log(2) + log(3) + log(4) + log(n) < log(n) + log(n) + log(n) + log(n) + log(n)
= n* log(n)
n * log(n) >= n * log(n) and that is Ω(n log n)
